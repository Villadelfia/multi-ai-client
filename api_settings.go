package multi_ai_client

// APISettings is a struct representing the settings needed to interact with an API.
// It contains the API key, the API endpoint, and the API type.
type APISettings struct {
	APIKey      string
	APIEndpoint string
	APIType     APIType
}
